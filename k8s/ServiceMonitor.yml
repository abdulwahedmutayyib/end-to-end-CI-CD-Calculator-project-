apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: default-apps-monitor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - default
  selector: {}
  endpoints:
  - port: http
    interval: 15s
